'use client';
import { useState } from 'react';
import { ENGINEERING_PROGRAMS } from '@/lib/engineering-catalog';

export default function Register(){
  const [name,setName]=useState(''); const [email,setEmail]=useState(''); const [password,setPassword]=useState('');
  const [targetRole,setTargetRole]=useState('Software Developer'); const [department,setDepartment]=useState('CSE'); const [msg,setMsg]=useState('');
  const selected=ENGINEERING_PROGRAMS.find(d=>d.code===department)||ENGINEERING_PROGRAMS[0];
  async function submit(e:React.FormEvent){e.preventDefault();setMsg('Creating account…');const r=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,email,password,targetRole,department})});const d=await r.json();if(r.ok)location.href='/learn';else setMsg(d.error||'Something went wrong.');}
  return <main className="container"><nav className="nav"><b className="brand">INTERVIEW</b><a href="/">Home</a></nav><form className="card form" onSubmit={submit}><div className="eyebrow">Student access</div><h1>Create your account</h1><p className="muted">Select your exact engineering program. INTERVIEW will build your learning roadmap from this selection.</p><label>Full name</label><input required minLength={2} value={name} onChange={e=>setName(e.target.value)}/><label>Email</label><input required type="email" value={email} onChange={e=>setEmail(e.target.value)}/><label>Engineering department / program</label><select value={department} onChange={e=>setDepartment(e.target.value)}>{ENGINEERING_PROGRAMS.map(p=><option key={p.code} value={p.code}>{p.code} — {p.title}</option>)}</select><div className="notice" style={{marginTop:8}}><b>{selected.title}</b><p className="muted">{selected.family} • {selected.tracks.length} learning track(s) • {selected.tracks.reduce((n,t)=>n+t.modules.length,0)} modules</p></div><label>Target role</label><select value={targetRole} onChange={e=>setTargetRole(e.target.value)}><option>Software Developer</option><option>Frontend Developer</option><option>Backend Developer</option><option>Java Developer</option><option>Data Analyst</option><option>AI/ML Engineer</option><option>Embedded Engineer</option><option>Core Engineer</option></select><label>Password</label><input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="btn full" type="submit">Create student account & start learning</button>{msg&&<p className="toast danger">{msg}</p>}<p className="muted">Already registered? <a href="/login"><b>Sign in</b></a></p></form></main>
}
