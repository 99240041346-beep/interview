'use client';
import {useState} from 'react';
const departments=['CSE','AIML','AI&DS','ECE','EEE','MECH','CIVIL','IT','CYS','EIE'];
export default function Register(){const[name,setName]=useState('');const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[targetRole,setTargetRole]=useState('Software Developer');const[department,setDepartment]=useState('CSE');const[msg,setMsg]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setMsg('Creating account…');const r=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,email,password,targetRole,department})});const d=await r.json();if(r.ok)location.href='/dashboard';else setMsg(d.error||'Something went wrong.')}return <main className="container"><nav className="nav"><b className="brand">INTERVIEW</b><a href="/">Home</a></nav><form className="card form" onSubmit={submit}><div className="eyebrow">Student access</div><h1>Create your account</h1><p className="muted">Choose your department to unlock the right learning modules.</p><label>Full name</label><input required minLength={2} value={name} onChange={e=>setName(e.target.value)}/><label>Email</label><input required type="email" value={email} onChange={e=>setEmail(e.target.value)}/><label>Department</label><select value={department} onChange={e=>setDepartment(e.target.value)}>{departments.map(d=><option key={d}>{d}</option>)}</select><label>Target role</label><select value={targetRole} onChange={e=>setTargetRole(e.target.value)}><option>Software Developer</option><option>Frontend Developer</option><option>Backend Developer</option><option>Java Developer</option><option>Data Analyst</option><option>AI/ML Engineer</option><option>Embedded Engineer</option><option>Core Engineer</option></select><label>Password</label><input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="btn full" type="submit">Create student account</button>{msg&&<p className="toast danger">{msg}</p>}<p className="muted">Already registered? <a href="/login"><b>Sign in</b></a></p></form></main>}
