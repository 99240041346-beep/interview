'use client';
import {useState} from 'react';
export default function Login(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[msg,setMsg]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setMsg('Signing in…');const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const d=await r.json();if(r.ok)location.href='/dashboard';else setMsg(d.error||'Unable to sign in.')}return <main className="container"><nav className="nav"><b className="brand">INTERVIEW</b><a href="/">Home</a></nav><form className="card form" onSubmit={submit}><div className="eyebrow">Student access</div><h1>Welcome back</h1><p className="muted">Continue your interview preparation.</p><label>Email</label><input required type="email" value={email} onChange={e=>setEmail(e.target.value)}/><label>Password</label><input required type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="btn full" type="submit">Sign in</button>{msg&&<p className="toast danger">{msg}</p>}<p className="muted">New here? <a href="/register"><b>Create a student account</b></a></p></form></main>}
